GEMINI_API_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
JWT_SECRET_KEY=

# Only needed in production. Locally this defaults to ./travel_planner.db.
# On Render, set to sqlite:////var/data/travel_planner.db (the mounted disk).
DATABASE_URL=

# Comma-separated list of origins allowed to call this API.
CORS_ORIGINS=https://multi-agents-travel-planner.vercel.app,http://localhost:5173,http://127.0.0.1:5173
